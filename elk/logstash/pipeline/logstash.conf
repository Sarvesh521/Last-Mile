input {
  beats {
    port => 5044
  }
}

filter {
  if [message] =~ "^{.*}$" {
    json {
      source => "message"
    }
  }
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "lastmile-logs-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
